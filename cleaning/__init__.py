"""Reproducible cleaning pipeline: ``data/raw`` -> ``data/processed``.

Run the whole pipeline from the repository root with::

    python -m cleaning.run_all

and verify the result (rebuild from scratch + integrity checks) with::

    python -m cleaning.verify
"""
